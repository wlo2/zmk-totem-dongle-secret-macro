description: |
  nRF Bootloader behavior using Zephyr Boot Mode API for reliable bootloader entry

compatible: "zmk,behavior-bootloader-nrf"

include: base.yaml

properties:
  "#binding-cells":
    type: int
    const: 0