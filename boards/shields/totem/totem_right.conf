# Battery reporting disabled for now.
# Without it the half does not sample the divider or expose a BAS service, so
# the central has nothing to fetch -- keep
# CONFIG_ZMK_SPLIT_BLE_CENTRAL_BATTERY_LEVEL_FETCHING off in totem_dongle.conf
# to match.
CONFIG_ZMK_BATTERY_REPORTING=n
